<!DOCTYPE html>
<html class="light" lang="en">
<head>
    <meta charset="utf-8"/>
    <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
    <title>Request a Custom Quote</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link href="https://fonts.googleapis.com" rel="preconnect"/>
    <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect"/>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet"/>
    <style>
        .material-symbols-outlined {
            font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
        }
    </style>
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#135bec",
                        "background-light": "#f6f6f8",
                        "background-dark": "#101622",
                    },
                    fontFamily: {
                        "display": ["Inter", "sans-serif"]
                    },
                    borderRadius: {
                        "DEFAULT": "0.25rem",
                        "lg": "0.5rem",
                        "xl": "0.75rem",
                        "full": "9999px"
                    },
                },
            },
        }
    </script>
    <style>
        body {
            min-height: max(884px, 100dvh);
        }
    </style>
</head>
<body class="bg-background-light dark:bg-background-dark font-display text-[#1A1A1A] dark:text-gray-200">
    <div class="relative flex h-auto min-h-screen w-full flex-col bg-background-light dark:bg-background-dark group/design-root overflow-x-hidden">
        
        <%- include ("header") -%>

        <div class="flex items-center bg-background-light dark:bg-background-dark p-4 pb-2 justify-between sticky top-0 z-10">
            <h1 class="text-[#1A1A1A] dark:text-gray-200 text-lg font-bold leading-tight tracking-[-0.015em] flex-1 text-center pr-12">Request a Quote</h1>
        </div>
        
        <div class="p-4 pt-0">
            <p class="text-center text-gray-600 dark:text-gray-400 text-sm">Tell us about your project, and we'll get back to you with an estimate within 24 hours.</p>
        </div>

        <!-- Quote Form -->
        <form method="POST" action="/quote" class="flex flex-col gap-8 p-4 pb-24">
            
            <!-- Step 1: Your Project -->
            <section class="flex flex-col gap-4">
                <div class="flex flex-col gap-2">
                    <p class="text-base font-medium leading-normal text-[#1A1A1A] dark:text-gray-200">Step 1: Your Project</p>
                    <div class="w-full rounded bg-gray-200 dark:bg-gray-700">
                        <div class="h-2 rounded bg-primary" style="width: 33%;"></div>
                    </div>
                </div>

                <label class="flex flex-col min-w-40 flex-1">
                    <p class="text-base font-medium leading-normal pb-2 text-[#1A1A1A] dark:text-gray-200">Project Type <span class="text-red-500">*</span></p>
                    <select name="projectType" required class="form-select flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-lg text-[#1A1A1A] dark:text-gray-200 focus:outline-0 focus:ring-2 focus:ring-primary/50 border border-gray-300 dark:border-gray-600 bg-background-light dark:bg-background-dark focus:border-primary dark:focus:border-primary h-14 placeholder:text-gray-500 dark:placeholder:text-gray-400 p-[15px] text-base font-normal leading-normal">
                        <option value="">Select project type</option>
                        <option value="Web Application">Web Application</option>
                        <option value="Mobile Application">Mobile Application</option>
                        <option value="AI / LLM Solution">AI / LLM Solution</option>
                        <option value="EdTech Platform">EdTech Platform</option>
                        <option value="Full-stack Development">Full-stack Development</option>
                        <option value="IT Consultancy">IT Consultancy</option>
                        <option value="Other">Other</option>
                    </select>
                </label>

                <label class="flex flex-col min-w-40 flex-1">
                    <p class="text-base font-medium leading-normal pb-2 text-[#1A1A1A] dark:text-gray-200">Project Description <span class="text-red-500">*</span></p>
                    <textarea name="description" required class="form-textarea flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-lg text-[#1A1A1A] dark:text-gray-200 focus:outline-0 focus:ring-2 focus:ring-primary/50 border border-gray-300 dark:border-gray-600 bg-background-light dark:bg-background-dark focus:border-primary dark:focus:border-primary min-h-36 placeholder:text-gray-500 dark:placeholder:text-gray-400 p-[15px] text-base font-normal leading-normal" placeholder="Tell us more about what you want to build... Include features, goals, target audience, and any specific requirements."></textarea>
                    <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">The more details you provide, the more accurate our quote will be.</p>
                </label>
            </section>

            <!-- Step 2: Scope & Timeline -->
            <section class="flex flex-col gap-4">
                <div class="flex flex-col gap-2">
                    <p class="text-base font-medium leading-normal text-[#1A1A1A] dark:text-gray-200">Step 2: Scope & Timeline</p>
                    <div class="w-full rounded bg-gray-200 dark:bg-gray-700">
                        <div class="h-2 rounded bg-primary" style="width: 66%;"></div>
                    </div>
                </div>

                <label class="flex flex-col min-w-40 flex-1">
                    <p class="text-base font-medium leading-normal pb-2 text-[#1A1A1A] dark:text-gray-200">Estimated Budget <span class="text-red-500">*</span></p>
                    <select name="budget" required class="form-select flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-lg text-[#1A1A1A] dark:text-gray-200 focus:outline-0 focus:ring-2 focus:ring-primary/50 border border-gray-300 dark:border-gray-600 bg-background-light dark:bg-background-dark focus:border-primary dark:focus:border-primary h-14 placeholder:text-gray-500 dark:placeholder:text-gray-400 p-[15px] text-base font-normal leading-normal">
                        <option value="">Select your budget range</option>
                        <option value="< $5,000">Less than $5,000</option>
                        <option value="$5,000 - $10,000">$5,000 - $10,000</option>
                        <option value="$10,000 - $25,000">$10,000 - $25,000</option>
                        <option value="$25,000 - $50,000">$25,000 - $50,000</option>
                        <option value="$50,000 - $100,000">$50,000 - $100,000</option>
                        <option value="$100,000+">$100,000+</option>
                    </select>
                </label>

                <div class="flex flex-col gap-2">
                    <label class="text-base font-medium leading-normal text-[#1A1A1A] dark:text-gray-200" for="timeline-range">
                        Desired Timeline <span class="text-red-500">*</span>
                    </label>
                    <input name="timeline" required class="w-full h-2 bg-gray-200 dark:bg-gray-700 rounded-lg appearance-none cursor-pointer accent-primary" id="timeline-range" min="1" max="4" type="range" value="2"/>
                    <div class="flex justify-between text-xs text-gray-500 dark:text-gray-400">
                        <span>1-3 Months</span>
                        <span>3-6 Months</span>
                        <span>6-9 Months</span>
                        <span>9+ Months</span>
                    </div>
                    <input type="hidden" id="timeline-value" name="timelineValue" value="3-6 Months"/>
                </div>
            </section>

            <!-- Step 3: Your Contact Info -->
            <section class="flex flex-col gap-4">
                <div class="flex flex-col gap-2">
                    <p class="text-base font-medium leading-normal text-[#1A1A1A] dark:text-gray-200">Step 3: Your Contact Info</p>
                    <div class="w-full rounded bg-gray-200 dark:bg-gray-700">
                        <div class="h-2 rounded bg-primary" style="width: 100%;"></div>
                    </div>
                </div>

                <label class="flex flex-col min-w-40 flex-1">
                    <p class="text-base font-medium leading-normal pb-2 text-[#1A1A1A] dark:text-gray-200">Full Name <span class="text-red-500">*</span></p>
                    <input name="name" required class="form-input flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-lg text-[#1A1A1A] dark:text-gray-200 focus:outline-0 focus:ring-2 focus:ring-primary/50 border border-gray-300 dark:border-gray-600 bg-background-light dark:bg-background-dark focus:border-primary dark:focus:border-primary h-14 placeholder:text-gray-500 dark:placeholder:text-gray-400 p-[15px] text-base font-normal leading-normal" placeholder="John Doe" type="text"/>
                </label>

                <label class="flex flex-col min-w-40 flex-1">
                    <p class="text-base font-medium leading-normal pb-2 text-[#1A1A1A] dark:text-gray-200">Work Email <span class="text-red-500">*</span></p>
                    <input name="email" required class="form-input flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-lg text-[#1A1A1A] dark:text-gray-200 focus:outline-0 focus:ring-2 focus:ring-primary/50 border border-gray-300 dark:border-gray-600 bg-background-light dark:bg-background-dark focus:border-primary dark:focus:border-primary h-14 placeholder:text-gray-500 dark:placeholder:text-gray-400 p-[15px] text-base font-normal leading-normal" placeholder="you@company.com" type="email"/>
                </label>

                <label class="flex flex-col min-w-40 flex-1">
                    <div class="flex justify-between items-baseline pb-2">
                        <p class="text-base font-medium leading-normal text-[#1A1A1A] dark:text-gray-200">Company Name</p>
                        <span class="text-sm text-gray-500 dark:text-gray-400">Optional</span>
                    </div>
                    <input name="company" class="form-input flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-lg text-[#1A1A1A] dark:text-gray-200 focus:outline-0 focus:ring-2 focus:ring-primary/50 border border-gray-300 dark:border-gray-600 bg-background-light dark:bg-background-dark focus:border-primary dark:focus:border-primary h-14 placeholder:text-gray-500 dark:placeholder:text-gray-400 p-[15px] text-base font-normal leading-normal" placeholder="Your Company Inc." type="text"/>
                </label>

                <label class="flex flex-col min-w-40 flex-1">
                    <p class="text-base font-medium leading-normal pb-2 text-[#1A1A1A] dark:text-gray-200">Phone Number</p>
                    <input name="phone" class="form-input flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-lg text-[#1A1A1A] dark:text-gray-200 focus:outline-0 focus:ring-2 focus:ring-primary/50 border border-gray-300 dark:border-gray-600 bg-background-light dark:bg-background-dark focus:border-primary dark:focus:border-primary h-14 placeholder:text-gray-500 dark:placeholder:text-gray-400 p-[15px] text-base font-normal leading-normal" placeholder="+1 (234) 567-8900" type="tel"/>
                </label>
            </section>

            <!-- Submit Button -->
            <div class="sticky bottom-0 p-4 bg-background-light dark:bg-background-dark border-t border-gray-200 dark:border-gray-700 -mx-4 -mb-4">
                <button type="submit" class="w-full bg-primary text-white font-bold py-4 px-4 rounded-lg text-lg flex items-center justify-center gap-2 hover:bg-blue-700 transition-colors disabled:opacity-50 disabled:cursor-not-allowed" id="submit-btn">
                    <span id="btn-text">Get My Quote</span>
                    <span id="loading-spinner" class="hidden">
                        <svg class="animate-spin h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                        </svg>
                    </span>
                </button>
                <p class="text-xs text-gray-500 dark:text-gray-400 mt-3 text-center">
                    By submitting, you agree to our <a class="text-primary underline hover:text-blue-700" href="/privacy">Privacy Policy</a>.
                    We'll contact you within 24 hours.
                </p>
            </div>
        </form>
    </div>

    <%- include ("footer") -%>

    <script>
        // Timeline range to text conversion
        const timelineRange = document.getElementById('timeline-range');
        const timelineValue = document.getElementById('timeline-value');
        const timelineLabels = ['1-3 Months', '3-6 Months', '6-9 Months', '9+ Months'];
        
        timelineRange.addEventListener('input', function() {
            const value = parseInt(this.value) - 1;
            timelineValue.value = timelineLabels[value] || '3-6 Months';
        });

        // Set initial value
        timelineValue.value = timelineLabels[parseInt(timelineRange.value) - 1];

        // Form submission handling
        const form = document.querySelector('form');
        const submitBtn = document.getElementById('submit-btn');
        const btnText = document.getElementById('btn-text');
        const loadingSpinner = document.getElementById('loading-spinner');

        form.addEventListener('submit', function(e) {
            // Update timeline value before submission
            const rangeValue = parseInt(timelineRange.value) - 1;
            timelineValue.value = timelineLabels[rangeValue];
            
            // Show loading state
            btnText.textContent = 'Processing...';
            loadingSpinner.classList.remove('hidden');
            submitBtn.disabled = true;
            
            // Form will submit normally via POST
        });

        // Auto-hide error message after 5 seconds
        const errorMessage = document.querySelector('.fixed.top-20');
        if (errorMessage) {
            setTimeout(() => {
                errorMessage.style.transition = 'opacity 0.5s ease';
                errorMessage.style.opacity = '0';
                setTimeout(() => errorMessage.remove(), 500);
            }, 5000);
        }

        // Add animation for form sections
        document.addEventListener('DOMContentLoaded', function() {
            const sections = document.querySelectorAll('section');
            sections.forEach((section, index) => {
                section.style.opacity = '0';
                section.style.transform = 'translateY(20px)';
                section.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                
                setTimeout(() => {
                    section.style.opacity = '1';
                    section.style.transform = 'translateY(0)';
                }, 100 * (index + 1));
            });
        });
    </script>

    <style>
        @keyframes fade-in {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .animate-fade-in {
            animation: fade-in 0.3s ease-out;
        }
        
        /* Custom range slider */
        input[type="range"]::-webkit-slider-thumb {
            appearance: none;
            height: 24px;
            width: 24px;
            border-radius: 50%;
            background: #135bec;
            cursor: pointer;
            border: 3px solid white;
            box-shadow: 0 2px 6px rgba(0,0,0,0.2);
        }
        
        input[type="range"]::-moz-range-thumb {
            height: 24px;
            width: 24px;
            border-radius: 50%;
            background: #135bec;
            cursor: pointer;
            border: 3px solid white;
            box-shadow: 0 2px 6px rgba(0,0,0,0.2);
        }
        
        /* Form focus styles */
        .form-input:focus, .form-select:focus, .form-textarea:focus {
            box-shadow: 0 0 0 3px rgba(19, 91, 236, 0.1);
        }
        
        /* Required field asterisk */
        .text-red-500 {
            color: #ef4444;
        }
    </style>
</body>
</html>